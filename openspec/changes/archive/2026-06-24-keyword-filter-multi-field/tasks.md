## 1. 前端：KEYWORDS 設定重構

- [x] 1.1 在 `frontend/src/App.jsx` 將 `KEYWORDS` 常數從字串陣列改為物件陣列，每個物件含 `label`（按鈕文字）與 `fields`（要比對的欄位名稱陣列）
- [x] 1.2 「醫院」的 `fields` 設定為 `['bid_name', 'agency']`，其餘三個設定為 `['bid_name']`

## 2. 前端：篩選邏輯更新

- [x] 2.1 更新 `filteredBids` 的排除邏輯：對每個啟用的關鍵字，改從 `KEYWORDS` 找出對應設定，並依 `fields` 逐一比對欄位，任一欄位命中即排除該列
- [x] 2.2 確認 `toggleKeyword` 與按鈕渲染邏輯仍使用 `label` 作為 key，無需修改

## 3. 驗收測試

- [x] 3.1 啟用「醫院」按鈕後，確認機關名稱含「醫院」的標案（即使標案名稱不含「醫院」）也被排除
- [x] 3.2 啟用「醫院」按鈕後，確認標案名稱含「醫院」的標案也被排除
- [x] 3.3 啟用「設備」按鈕後，確認只有標案名稱含「設備」的列被排除，機關名稱含「設備」（若有）的列不受影響
- [x] 3.4 同時啟用「醫院」與「設備」，確認 OR 排除邏輯正確運作
